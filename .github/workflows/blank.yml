name: Hello world
on: 
  push:
    branches: 
      - main
      - develop
  pull_request:
    branches:
      -main
jobs:
  build: 
    runs-on: ubuntu-latest
    steps:
      - name: Build step
        run: echo "A demo ${{vars.MY_NAME}}"
